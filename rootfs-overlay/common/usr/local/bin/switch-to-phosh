#!/bin/bash
if [ "$(id -u)" -ne 0 ]; then
    echo "Please run as root or with sudo"
    exit 1
fi
sed -i 's/^Session=.*/Session=phosh/' /var/lib/AccountsService/users/ubuntu
sed -i 's/^AutomaticLoginSession.*/AutomaticLoginSession = phosh/' /etc/gdm3/custom.conf
echo "Switched default session to Phosh. Please restart GDM or reboot."
